---
description: Keyword trigger for the #critic edge-case discovery workflow. Always loaded, minimal cost.
alwaysApply: true
---

If the user's message contains `#critic`, immediately read `.cursor/rules/agent-critic.mdc` in full and follow every instruction in it before doing anything else.
